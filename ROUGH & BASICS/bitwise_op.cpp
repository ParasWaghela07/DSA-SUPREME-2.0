#include<iostream>
using namespace std;

// int yes=10;   initialiazation of global variable

int main(){
    
    // int a=5;
    // int b=10;

//convert both elements in binary then AND,OR,XOR them//

    // cout<<"AND of 5 and 10 : "<<(a & b)<<endl;

    // cout<<"OR of 5 and 10  : "<<(a | b)<<endl;

    // int num=1;
    // cout<<"negation of 1  : "<<~(num)<<endl;        //same
    // cout<<"negation of 1  : "<<~num<<endl;         //same

    // int x=5;
    // int y=5;
    // int z=10;

    // cout<<"XOR of 5 and 5  : "<<(x^y)<<endl;

    // cout<<"XOR of 5 and 10 : "<<(x^z)<<endl;

    //left shfit , right shift
    // |
    // |
    // |
    // |
    // V

    // by performing left shift  as a<<n
    // then 'a' will be multiplied by 2^n

    // int a=7;
    // int ans=(a<<4);
    // cout<<ans<<endl;

    //right shfit , right shift
    // |
    // |
    // |
    // |
    // V

    // by performing right shift  as a>>n               in case of signed integer only , for unsigned large +ve value will occur
    // then 'a' will be divide by 2^n                   if shift by -Ve number ans will be garbage value

    // int a=20;
    // int ans=(a>>2);
    // cout<<ans<<endl;

    // pre/post increment/decrement operators
    // |
    // |
    // |
    // |
    // V

    //int a=5;
    // cout<<(++a);   ans=6, current=6
    // cout<<(a++);      ans=5, current=6

    // int a=10;
    // cout<<((a++)*(++a));  120

    // cout<<((++a)*(a++));  121/132

// cout<<yes;   //global variable
}